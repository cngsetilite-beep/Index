ob_start(function($buffer) {
    $buffer = preg_replace('/\>\s+\</', '><', $buffer);
    $buffer = preg_replace('/\s+/', ' ', $buffer);
    $buffer = str_replace("\n", "", $buffer);
    return $buffer;
});
